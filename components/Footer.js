import styles from "../styles/footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      XL do not claim any of these codes as original
    </footer>
  );
};

export default Footer;
